{
    "name": "backend",
    "version": "1.0.0",
    "description": "",
    "main": "app.js",
    "scripts": {
        "start": "node app",
        "dev": "nodemon app",
        "build": "yarn run build:1 && yarn run build:2",
        "build:1": "cd ../frontend && yarn install && yarn run build",
        "build:2": "cd ../whiteboard-root && yarn install && yarn run build"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "accesscontrol": "^2.2.1",
        "axios": "^0.21.4",
        "dotenv": "^10.0.0",
        "express": "^4.17.1",
        "konva": "^8.1.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.12"
    }
}

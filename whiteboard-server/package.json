{
    "name": "whiteboard-server",
    "version": "1.0.0",
    "description": "",
    "main": "app.js",
    "scripts": {
        "dev": "node app",
        "test": "nodemon app",
        "build": "yarn build-step-1",
        "build-step-1": "cd ../whiteboard-client && yarn install && yarn build && cd ../whiteboard-server && yarn build-step-2",
        "build-step-2": "cd ../spa-root && yarn install && yarn build"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "accesscontrol": "^2.2.1",
        "axios": "^0.21.4",
        "dotenv": "^10.0.0",
        "express": "4.17.1",
        "konva": "^8.1.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.12"
    }
}

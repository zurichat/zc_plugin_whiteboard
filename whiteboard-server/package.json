{
    "name": "whiteboard-server",
    "version": "1.0.0",
    "description": "",
    "main": "app.js",
    "scripts": {
        "dev": "node app",
        "test": "nodemon app",
        "build": "yarn install && cd ../whiteboard-client && yarn install && yarn run build"

    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "accesscontrol": "^2.2.1",
        "axios": "^0.21.4",
        "dotenv": "^10.0.0",
        "express": "^4.17.1",
        "konva": "^8.1.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.12"
    }
}
